import { guidedImpersonate } from './guidedImpersonate.js';

// Placeholder for 3rd person impersonation
// Currently just calls the main impersonate function
export function guidedImpersonate3rd() {
    console.log("Guided Impersonate (3rd Person) triggered.");
    // Potentially call guidedImpersonate with a specific mode/parameter later
    guidedImpersonate(); 
}
